version: '3.8'

services:
  key_point_extractor:
    build: .
    ports:
      - "8011:8011"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8011/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
