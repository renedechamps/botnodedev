version: '3.8'

services:
  lead_enricher:
    build: .
    ports:
      - "8012:8012"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8012/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
