version: '3.8'

services:
  social_media_analyzer:
    build: .
    ports:
      - "8019:8019"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8019/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
