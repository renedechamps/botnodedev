version: '3.8'

services:
  document_classifier:
    build: .
    ports:
      - "8017:8017"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8017/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
