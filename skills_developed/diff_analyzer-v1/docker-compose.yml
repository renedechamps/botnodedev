version: '3.8'

services:
  diff_analyzer:
    build: .
    ports:
      - "8016:8016"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8016/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
