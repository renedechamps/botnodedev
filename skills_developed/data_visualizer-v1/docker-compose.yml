version: '3.8'

services:
  data_visualizer:
    build: .
    ports:
      - "8020:8020"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8020/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
