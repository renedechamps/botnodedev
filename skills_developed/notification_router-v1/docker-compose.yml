version: '3.8'

services:
  notification_router:
    build: .
    ports:
      - "8014:8014"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8014/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
